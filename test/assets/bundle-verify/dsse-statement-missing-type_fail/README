The otherwise valid DSSE bundle contains an in-toto statement without the
required _type field. Verification must fail rather than interpreting it as a
v1 statement.

Creation: use the same one-leaf managed-key construction described in
`../dsse-statement-unknown-type_fail/README`, but omit `_type` from the signed
statement.
